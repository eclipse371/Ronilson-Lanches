<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <title>Editar Lanche</title>
  <style>
    body {
      font-family: 'Arial', sans-serif;
      background-color: #f4f4f4;
      margin: 0;
      padding: 0;
    }

    h1 {
      text-align: center;
      color: #333;
      margin: 30px 0;
    }

    form#form-editar-lanche {
      background-color: #fff;
      width: 90%;
      max-width: 500px;
      margin: 20px auto 40px auto;
      padding: 25px 30px;
      border-radius: 8px;
      box-shadow: 0 4px 8px rgba(0,0,0,0.1);
    }

    form#form-editar-lanche label {
      display: block;
      font-weight: bold;
      margin-bottom: 6px;
      color: #333;
      font-size: 1em;
    }

    form#form-editar-lanche input[type="text"],
    form#form-editar-lanche input[type="number"],
    form#form-editar-lanche textarea {
      width: 100%;
      padding: 10px 12px;
      margin-bottom: 20px;
      border: 1px solid #ccc;
      border-radius: 4px;
      font-size: 1em;
      font-family: Arial, sans-serif;
      box-sizing: border-box;
      transition: border-color 0.3s ease;
    }

    form#form-editar-lanche input[type="text"]:focus,
    form#form-editar-lanche input[type="number"]:focus,
    form#form-editar-lanche textarea:focus {
      border-color: #4CAF50;
      outline: none;
    }

    form#form-editar-lanche textarea {
      resize: vertical;
      min-height: 80px;
    }

    #btn-editar {
      background-color: #3498db;
      color: white;
      padding: 12px 20px;
      font-size: 1em;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      transition: background-color 0.3s ease;
      display: block;
      width: 100%;
    }

    #btn-editar:hover {
      background-color: #2980b9;
    }

    a {
      display: block;
      width: 90%;
      max-width: 500px;
      margin: 0 auto 40px auto;
      text-align: center;
      text-decoration: none;
      color: #3498db;
      font-weight: 600;
      font-size: 1em;
      transition: color 0.3s ease;
    }

    a:hover {
      color: #2980b9;
    }

    @media (max-width: 768px) {
      form#form-editar-lanche {
        width: 95%;
        padding: 20px;
      }

      #btn-editar {
        font-size: 0.9em;
        padding: 10px;
      }

      a {
        font-size: 0.9em;
      }
    }
  </style>
</head>
<body>
  <h1>Editar Lanche</h1>

  <form id="form-editar-lanche" action="/lista/editar/<%= lanche.id %>" method="POST">
      <label for="nome">Nome:</label>
      <input type="text" id="nome" name="nome" value="<%= lanche.nome %>" required />

      <label for="descricao">Descrição:</label>
      <textarea id="descricao" name="descricao" required><%= lanche.descricao %></textarea>

      <label for="preco">Preço:</label>
      <input type="number" step="0.01" id="preco" name="preco" value="<%= lanche.preco %>" required />

      <button id="btn-editar" data-id="<%= lanche.id %>" type="button">Salvar Alterações</button>
  </form>

  <a href="/lista">Voltar para lista</a>
  <script src="/js/editar.js"></script>

</body>
</html>
